# 复制本文件为 .env 后填值（.env 不要提交到 git）
# docker compose 会自动读取同目录的 .env

# 钉钉自定义机器人（群设置→智能群助手→添加机器人→自定义，安全设置选「加签」）
DINGTALK_TOKEN=替换成机器人URL里access_token=后面那串
DINGTALK_SECRET=替换成加签密钥SECxxxx

# 代码仓库（本机 demo 阶段可先随便填，等接真仓库再换）
# GitLab：建只读 Access Token（read_api + read_repository）
# GitHub：换成 PAT，并把工作流里 search_code/read_file/list_commits 的 URL 改成 api.github.com
GL_TOKEN=替换成只读token
GL_PROJECT_ID=替换成项目数字ID

# 说明：Anthropic API Key 不放这里，而是在 n8n 界面里建 Anthropic Credential 时填，
#       更符合 n8n 凭据管理的习惯，也不会出现在 docker 进程环境里。
