# 05-前端自检报告 — 前后端分离个人博客系统

> **需求代号**：blog  
> **开发人**：前端开发 (frontend-dev)  
> **开发日期**：2026-06-12  
> **文档版本**：v1.0

---

## 一、技术栈

| 类别 | 技术 | 版本 | 说明 |
|:-----|:-----|:-----|:-----|
| 框架 | Vue 3 (Composition API) | 3.4+ | `<script setup>` 语法 |
| 语言 | TypeScript | 5.4+ | 全量类型定义 |
| 状态管理 | Pinia | 2.1+ | 支持持久化插件 |
| UI 组件库 | Element Plus | 2.7+ | 中文 locale |
| 路由 | Vue Router 4 | 4.3+ | 懒加载 + 路由守卫 |
| HTTP 客户端 | Axios | 1.7+ | 请求/响应拦截器封装 |
| Markdown 编辑器 | ByteMD (Vue 3) | 1.21+ | 管理后台文章编辑 |
| Markdown 渲染 | 后端预渲染 HTML | — | 前端 v-html 展示 |
| 代码高亮 | Prism.js | 1.29+ | 文章详情代码块高亮 |
| 构建工具 | Vite | 5.4+ | HMR 开发体验 |
| SSG | Nuxt 3 | 3.12+ | SEO 静态生成 |
| Sitemap | @nuxtjs/sitemap | 5.0+ | 自动生成 sitemap.xml |

---

## 二、功能完成情况

### 2.1 blog-frontend（博客前端）

| 功能点 | 状态 | 对应 P 级别 | 备注 |
|:-------|:----:|:----------:|:-----|
| 文章列表（分页） | ✅ | P0 | 支持分页、按发布时间/阅读量排序 |
| 分类筛选 | ✅ | P0 | 按分类过滤文章列表 |
| 标签筛选 | ✅ | P0 | 按标签过滤文章列表 |
| 文章详情页 | ✅ | P0 | Markdown 渲染 + Prism.js 代码高亮 |
| 文章详情 SEO TDK | ✅ | P0 | 动态设置 title/description/keywords |
| 全文搜索 | ✅ | P0 | 关键词搜索 + 结果高亮 + 相关度展示 |
| 文章归档 | ✅ | P1 | 按年月分组展示时间线 |
| 用户注册 | ✅ | P0 | 表单校验（用户名/密码/邮箱） |
| 用户登录 | ✅ | P0 | JWT Token 存储 + Pinia 状态管理 |
| 登录态持久化 | ✅ | P0 | localStorage + Pinia persistedstate |
| 评论提交 | ✅ | P0 | 嵌套回复支持、表单校验 |
| 评论展示 | ✅ | P0 | 2层嵌套结构、展开/收起回复 |
| 评论审核状态提示 | ✅ | P0 | 访客"评论已提交，审核通过后展示" |
| 标签云 | ✅ | P0 | 按文章数计算权重字号 |
| 友链展示 | ✅ | P1 | 友链卡片网格布局 |
| 关于页面 | ✅ | P1 | 技术栈展示 |
| 404 页面 | ✅ | — | 友好 404 提示 |
| 响应式布局 | ✅ | P1 | 移动端/平板/桌面端适配 |
| Loading/Empty/Error 状态 | ✅ | P0 | 三态全覆盖 |

### 2.2 blog-admin（管理后台）

| 功能点 | 状态 | 对应 P 级别 | 备注 |
|:-------|:----:|:----------:|:-----|
| 管理端登录 | ✅ | P0 | 角色校验（OWNER/ADMIN/AUTHOR） |
| 路由鉴权守卫 | ✅ | P0 | 未登录重定向到 /login |
| 仪表盘 | ✅ | P1 | 文章数/草稿/待审核评论/总访问量统计 |
| 文章列表管理 | ✅ | P0 | 状态筛选、分页、删除确认 |
| 文章创建/编辑 | ✅ | P0 | ByteMD 编辑器 + 分类/标签/封面图设置 |
| 保存草稿 | ✅ | P0 | DRAFT 状态保存 |
| 发布文章 | ✅ | P0 | PUBLISHED 状态发布 |
| 分类 CRUD | ✅ | P0 | 增删改查 + 弹窗表单 |
| 标签 CRUD | ✅ | P0 | 增删改查 + 弹窗表单 |
| 评论审核队列 | ✅ | P0 | 通过/拒绝/删除操作 |
| 图片上传 | ✅ | P1 | ByteMD 编辑器集成上传 |
| 用户管理 | ⬜ | P1 | 页面已预留，待后端接口 |

### 2.3 blog-nuxt（Nuxt 3 SSG 层）

| 功能点 | 状态 | 对应 P 级别 | 备注 |
|:-------|:----:|:----------:|:-----|
| 静态首页生成 | ✅ | P0 | Nuxt 3 SSG 预渲染文章列表 |
| 文章详情静态页 | ✅ | P0 | 动态路由 `[slug].vue` |
| TDK 动态设置 | ✅ | P0 | useHead + Meta 组件 |
| Open Graph 标签 | ✅ | P0 | og:title/description/type/article |
| Schema.org 结构化数据 | ✅ | P0 | Article schema JSON-LD |
| sitemap.xml 生成 | ✅ | P0 | @nuxtjs/sitemap 自动生成 |
| 文章归档页 | ✅ | P1 | 按年月分组展示 |
| robots.txt | ✅ | P0 | Nuxt 3 自动生成 |

---

## 三、接口联调情况

| 接口序号 | 接口名称 | 方法 | URL | 状态 | 备注 |
|:--------:|:---------|:----:|:----|:----:|:-----|
| 01 | 用户注册 | POST | `/api/auth/register` | ✅ | 已对接，含表单校验 |
| 02 | 用户登录 | POST | `/api/auth/login` | ✅ | 已对接，JWT Token 存储 |
| 03 | 文章列表（公开） | GET | `/api/articles` | ✅ | 已对接，分页/分类/标签筛选/排序 |
| 04 | 文章详情（公开） | GET | `/api/articles/{slug}` | ✅ | 已对接，v-html 渲染 + 代码高亮 |
| 05 | 文章管理 CRUD | GET/POST/PUT/DELETE | `/api/admin/articles` `/api/admin/articles/{id}` | ✅ | 已对接，ByteMD 编辑器；后端已补充 GET by ID（含 categoryId/tagIds/content 原文用于编辑器回填） |
| 06 | 分类与标签管理 | GET/POST/PUT/DELETE | `/api/admin/categories` `/tags` | ✅ | 已对接，分类树/标签云 |
| 07 | 评论提交与列表 | GET/POST | `/api/articles/{id}/comments` | ✅ | 已对接，嵌套回复 |
| 08 | 评论审核管理 | GET/PUT/DELETE | `/api/admin/comments` | ✅ | 已对接，通过/拒绝 |
| 09 | 全文搜索 | GET | `/api/search` | ✅ | 已对接，关键词高亮 |
| 10 | 文件上传 | POST | `/api/upload/image` | ✅ | 已对接，multipart/form-data |

**联调说明**：
- Axios 拦截器统一处理 `code !== 0` 业务错误和 HTTP 状态码错误
- 401 自动清除登录态并跳转登录页
- 429 提示"操作过于频繁"
- 请求超时设置 10s（上传 30s）

---

## 四、自检问题清单

| # | 问题 | 严重级别 | 状态 | 说明 |
|:--:|:-----|:--------:|:----:|:-----|
| 01 | Nuxt SSG 层需后端 API 可用时才能 `nuxt generate` | 🟡 中 | ✅ 已确认 | 后端已交付，构建时 API 可用 |
| 02 | 管理后台 admin-frontend 使用 `/admin/` base path，需 Nginx 配置配合 | 🟡 中 | ✅ 已确认 | devops 已提供完整 Nginx 配置（含 alias + SPA fallback + 静态资源缓存 + API 代理 + 限流），vite.config.ts 已补充 `base: '/admin/'` |
| 03 | ByteMD 编辑器图片上传需后端 `/api/upload/image` 接口就绪 | 🟡 中 | ✅ 已确认 | 后端已交付，接口就绪 |
| 04 | 友链 `/api/links` 接口未在 P0 文档中定义 | 🟢 低 | ✅ 已处理 | 前端做了降级处理，接口不存在时静默不报错 |
| 05 | 用户管理页面为占位符 | 🟢 低 | ✅ 已处理 | P1 功能，页面已预留，待后端接口就绪后对接 |
| 06 | blog-frontend 和 blog-admin 存在 API 层代码重复 | 🟢 低 | ✅ 已接受 | 两个独立项目便于独立部署，重复量可控 |
| 07 | Prism.js 语言包按需引入，后续可能缺少部分语言 | 🟢 低 | ✅ 已处理 | 已引入常见语言（Java/Python/JS/TS/JSON/SQL/Bash/YAML） |
| 08 | 移动端响应式未全面测试 | 🟡 中 | ⬜ 待 QA 验证 | 已使用 flex/grid 布局，理论上兼容 |

---

## 五、待确认问题

| # | 问题 | 建议 |
|:--:|:-----|:-----|
| Q1 | 管理后台 base path 为 `/admin/`，Nginx 配置是否需要调整？ | ✅ devops 已确认：`location /admin` + `alias` + `try_files` SPA fallback，Vite `base: '/admin/'` 已配置 |
| Q2 | Nuxt SSG 构建时 API 地址如何配置？ | 使用环境变量 `NUXT_PUBLIC_API_BASE`，默认 `http://127.0.0.1:8080/api`；构建命令：`NUXT_PUBLIC_API_BASE=https://your-api.com/api npx nuxt generate` |
| Q3 | 是否需要前端 Mock Server 用于独立开发？ | 建议 devops 配置 vite proxy 即可，开发阶段无需独立 Mock |

---

## 六、项目文件清单

### blog-frontend (21 个文件)

```
blog-frontend/
├── package.json
├── tsconfig.json
├── vite.config.ts
├── index.html
└── src/
    ├── env.d.ts
    ├── main.ts
    ├── App.vue
    ├── api/
    │   ├── request.ts          # Axios 封装 + 拦截器
    │   ├── auth.ts             # 认证 API
    │   ├── article.ts          # 文章 API
    │   ├── category.ts         # 分类/标签 API
    │   ├── comment.ts          # 评论 API
    │   ├── search.ts           # 搜索 API
    │   ├── upload.ts           # 上传 API
    │   └── index.ts            # 统一导出
    ├── types/
    │   └── index.ts            # 全局 TypeScript 类型
    ├── constants/
    │   └── index.ts            # 常量/枚举/校验规则
    ├── stores/
    │   ├── user.ts             # 用户状态（登录/注册/Token）
    │   ├── app.ts              # 应用状态（分类/标签缓存）
    │   └── index.ts
    ├── router/
    │   └── index.ts            # 路由配置 + 守卫
    ├── components/
    │   ├── AppHeader.vue       # 全局头部导航
    │   ├── AppFooter.vue       # 全局底部
    │   ├── ArticleCard.vue     # 文章卡片
    │   ├── SidebarWidget.vue   # 侧边栏小组件
    │   ├── TagCloud.vue        # 标签云
    │   ├── CommentItem.vue     # 评论项（递归组件）
    │   └── CommentForm.vue     # 评论表单
    └── pages/
        ├── Home.vue            # 首页（文章列表+筛选）
        ├── ArticleDetail.vue   # 文章详情（渲染+代码高亮+评论）
        ├── Search.vue          # 全文搜索
        ├── Archive.vue         # 文章归档
        ├── Login.vue           # 登录
        ├── Register.vue        # 注册
        ├── Links.vue           # 友链
        ├── About.vue           # 关于
        └── NotFound.vue        # 404
```

### blog-admin (19 个文件)

```
blog-admin/
├── package.json
├── tsconfig.json
├── vite.config.ts
├── index.html
└── src/
    ├── env.d.ts
    ├── main.ts
    ├── App.vue
    ├── api/
    │   ├── request.ts
    │   ├── auth.ts
    │   ├── article.ts
    │   ├── category.ts
    │   ├── comment.ts
    │   ├── upload.ts
    │   └── index.ts
    ├── types/
    │   └── index.ts
    ├── constants/
    │   └── index.ts
    ├── stores/
    │   └── user.ts             # 管理端用户状态（含角色校验）
    ├── router/
    │   └── index.ts            # 管理端路由 + 鉴权守卫
    └── pages/
        ├── AdminLayout.vue     # 管理布局（侧边栏+头部）
        ├── Login.vue           # 管理端登录
        ├── Dashboard.vue       # 仪表盘
        ├── ArticleList.vue     # 文章管理列表
        ├── ArticleEditor.vue   # 文章编辑器（ByteMD）
        ├── CategoryManage.vue  # 分类管理
        ├── TagManage.vue       # 标签管理
        ├── CommentReview.vue   # 评论审核队列
        ├── UserManage.vue      # 用户管理（占位）
        └── NotFound.vue        # 404
```

### blog-nuxt (11 个文件)

```
blog-nuxt/
├── package.json
├── nuxt.config.ts              # Nuxt 3 SSG 配置
├── tsconfig.json
├── app.vue                     # 根组件
├── assets/
│   └── css/
│       └── main.css
├── components/
│   ├── HeaderNav.vue
│   └── FooterNav.vue
├── pages/
│   ├── index.vue               # 首页（SSG 预渲染）
│   ├── archive.vue             # 归档页
│   └── article/
│       └── [slug].vue          # 文章详情（动态路由 + SSG）
└── utils/
    └── api.ts                  # API 工具函数
```

**总计**：3 个子项目，51 个源码文件。

---

## 七、代码规范遵循情况

| 规范项 | 遵循 |
|:-------|:----:|
| TypeScript 所有 props 有类型定义 | ✅ |
| 组件遵循单一职责原则 | ✅ |
| API 调用统一封装（Axios 拦截器） | ✅ |
| 配置化的错误提示（常量文件） | ✅ |
| 关键操作有 loading 状态 | ✅ |
| 表单实时校验 | ✅ |
| 删除操作二次确认（popconfirm） | ✅ |
| 注释解释"为什么"而非"做什么" | ✅ |
| loading / empty / error 三态覆盖 | ✅ |
| 接口超时、网络异常等边界处理 | ✅ |

---

> **文档结束** — 下一阶段：QA 测试人员基于本文档进行前端功能验证。
